import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  navbarDrawer: {
    width: '100%',
  },
  navbarToggle: {
    marginLeft: '1rem',
  },
  contentContainer: {
    padding: 0,
  },
}));
