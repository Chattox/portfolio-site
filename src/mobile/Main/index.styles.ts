import { createStyles } from '@mantine/core';

export const useStyles = createStyles(() => ({
  header: {
    zIndex: 999,
  },
  navbarDrawer: {
    width: '100%',
  },
  navbarContent: {
    paddingTop: '1rem',
  },
  navbarToggle: {
    marginLeft: '1rem',
  },
  contentContainer: {
    padding: 0,
  },
}));
